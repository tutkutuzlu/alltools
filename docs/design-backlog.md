# Design backlog

These improvements are intentionally deferred so the 19-tool production release is not delayed:

- Add centralized, reusable SVG icons tailored to each tool type.
- Reduce the mobile hero and vertical spacing by approximately 15%.
- Compact long action labels on mobile screens.
- Use an adaptive result textarea height for short outputs.
- Optimize mobile typography for long metric labels.
