# Unit Converters family

Unit Converters use a declarative catalog, a precision-aware conversion engine and one shared browser runtime. Each plugin supplies metadata and editorial content while the family owns conversion, validation, unit selection, swapping, clipboard actions and deduplicated telemetry.
