# Text Tools family

V1 text tools share a text-first interaction model: one primary text input, a pure analyzer or transformer function, common actions, and an accessible result area.

The family is intentionally small in V1. Word Counter validates the analyzer variant. Future members may add transformer and line-oriented variants without changing the plugin contract.
