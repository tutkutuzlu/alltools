# Developer Tools family

The family provides one shared runtime for browser-only formatters, parsers, validators, converters, generators and reference lookups. Individual plugins contain metadata, editorial content and a thin entry module; behavior is registered in `tool-definitions.js` and implemented in `operations.js`.
