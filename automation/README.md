# Future automation boundary

AI generation, discovery, evaluation and publishing automation are intentionally outside V1. Future systems must create or evaluate standard plugin folders and must not bypass the normal validation and build contracts.
